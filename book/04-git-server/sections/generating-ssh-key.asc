[[_generate_ssh_key]]
=== 生成你的 SSH 公钥

(((SSH keys)))
正如人们所说，许多 Git 服务器使用 SSH 公钥来进行认证。
为了向 Git 服务器提供一个 SSH 公钥，每个系统内的用户在没有 SSH 公钥的情况下必须自行生成。
这个操作在所有的操作系统上都是相似的。
首先，你得确认你是否已经拥有了一个密钥。
默认情况下，用户的 SSH 密钥存储在其 `~/.ssh` 目录下。
你可以简单地通过进入那个文件夹并列出其中的内容来检查你是否已经有了一个密钥：

[source,console]
----
$ cd ~/.ssh
$ ls
authorized_keys2  id_dsa       known_hosts
config            id_dsa.pub
----

你所寻找的一对文件其中一个的名字类似 `id_dsa` 或 `id_rsa`，与其对应的另一个则带有 `.pub` 扩展名。
带有 `.pub` 扩展名的文件是你的公钥，而另一个则是私钥。
如果你找不到这些文件（或者你根本没有 `.ssh` 目录），你可以通过运行 Linux/Mac 的 SSH 软件包中的 `ssh-keygen` 程序来创建密钥，在 Windows 上，这个程序包含于 MSysGit 软件包中。

[source,console]
----
$ ssh-keygen
Generating public/private rsa key pair.
Enter file in which to save the key (/home/schacon/.ssh/id_rsa):
Created directory '/home/schacon/.ssh'.
Enter passphrase (empty for no passphrase):
Enter same passphrase again:
Your identification has been saved in /home/schacon/.ssh/id_rsa.
Your public key has been saved in /home/schacon/.ssh/id_rsa.pub.
The key fingerprint is:
d0:82:24:8e:d7:f1:bb:9b:33:53:96:93:49:da:9b:e3 schacon@mylaptop.local
----

首先它会确认你想把密钥存储在什么地方（`.ssh/id_rsa`），之后它将要求你两次输入一个口令，如果你不想在使用密钥时输入一个口令，你可以将其置空。

现在，每个进行上述操作的用户需要将他们的公钥发送给你或者其他管理该 Git 服务器的人（假设你正在使用一个需要公钥的 SSH 服务器设置）。
他们只需将 `.pub` 文件的内容通过电子邮件发送出去即可。
公钥看起来是这样的：

[source,console]
----
$ cat ~/.ssh/id_rsa.pub
ssh-rsa  schacon@mylaptop.local
----

想要更深入地了解在多种操作系统中生成 SSH 密钥的方法，请参阅 GitHub 上关于 SSH 密钥的教程 https://help.github.com/articles/generating-ssh-keys[]。
